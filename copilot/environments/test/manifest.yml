name: test
type: Environment

observability:
  container_insights: false

count: 1

http:
  alias: ["staging.api.vigilion.com"]
  path: '/'
    # You can specify a custom health check path. The default is "/".
  healthcheck:
    path: '/healthcheck'
    success_codes: '200,301'
